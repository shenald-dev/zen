[project]
name = "zen-timer"
version = "0.1.7"
description = "🧘 Deep-work timer. Simple terminal app for focus sessions. No GUI, just flow."
authors = [
    { name = "shenald-dev", email = "bot@shenald.dev" }
]
requires-python = ">=3.10"
dependencies = [
<<<<<<< HEAD
    "rich>=13.9.0",
    "typer>=0.12.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-mock>=3.14.0",
    "pytest-cov>=5.0.0",
    "flake8>=7.0.0",
    "pylint>=3.0.0"
=======
    "rich>=14.3.3",
    "typer>=0.24.1"
>>>>>>> b9e9416 (chore(release): push release v0.1.7 tag)
]

[project.scripts]
zen = "zen.timer:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
